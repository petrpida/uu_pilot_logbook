import UuPilotLogbook from "uu_pilot_logbook-hi";
import { mount, wait } from "uu5g05-test";

describe(`UuPilotLogbook.Core.LogbookEntry.PilotObjectProvider`, () => {
  // it("onLoad", async () => {
  //   let onLoad = jest.fn(async () => ({ content: "Hello, world!" }));
  //   let loadDtoIn = { foo: "bar" };
  //   let wrapper = mount(<UuPilotLogbook.Core.LogbookEntry.PilotObjectProvider onLoad={onLoad} loadDtoIn={loadDtoIn} />);
  //   expect(onLoad).toHaveBeenCalledTimes(1);
  //   expect(onLoad).lastCalledWith(loadDtoIn);
  //   await wait();
  //   expect(wrapper.text()).toContain("Hello, world!");
  // });
});
